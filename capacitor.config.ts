import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jansamarpit',
  appName: 'Jansamarpit',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
